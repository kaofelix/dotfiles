# -*- coding: utf-8; mode:conf -*-
[user]
    name = Kao Félix
    email = kcfelix@gmail.com
[color]
    diff = auto
    status = auto
    branch = auto
    interactive = auto
[core]
    excludesfile = /Users/kaofelix/.gitignore
    autocrlf = input
[gui]
    fontdiff = -family Menlo -size 11 -weight normal -slant roman -underline 0 -overstrike 0
[alias]
    st = status
    ci = commit
    co = checkout
    br = branch
    l  = log
    lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --
    pr = pull --rebase
    pff = pull --ff-only
    amend = commit --amend
    fix = commit --amend --no-edit
    di = diff
    ds = diff --staged
    # ds = diff --stat=160,120
    dh1 = diff HEAD~1
    aa = add --all
    ff = merge --ff-only
    noff = merge --no-ff
    fa = fetch --all
    pom = push origin master
    spp = "stash && pull --rebase && stash pop"

    # Divergence (commits we added and commits remote added)
    div = divergence

    # Goodness (summary of diff lines added/removed/total)
    gn = goodness
    gnc = goodness --cached

    # Fancy logging.
    #   h = head
    #   hp = head with patch
    #   r = recent commits, only current branch
    #   ra = recent commits, all reachable refs
    #   l = all commits, only current branch
    #   la = all commits, all reachable refs
    head = !git r -1
    h = !git head
    hp = "!. ~/.githelpers && show_git_head"
    r = !GIT_NO_PAGER=1 git l -30
    ra = !git r --all
    l = "!. ~/.githelpers && pretty_git_log"
    la = !git l --all        
        
[github]
    user = kaofelix
    token = b22db9adf3e01b9af1c0bc6ae0fd1bd2
    oauth-token = 8f132bcfd2ab355fe42bb513f70dac125e43c496
[branch]
    autosetuprebase = always
[difftool]
    prompt = false
[difftool "Kaleidoscope"]
    cmd = ksdiff --partial-changeset --relative-path \"$MERGED\" -- \"$LOCAL\" \"$REMOTE\"
[diff]
    tool = Kaleidoscope
[mergetool]
    prompt = false
[mergetool "Kaleidoscope"]
    cmd = ksdiff --merge --output \"$MERGED\" --base \"$BASE\" -- \"$LOCAL\" --snapshot \"$REMOTE\" --snapshot
    trustExitCode = true
[merge]
    tool = Kaleidoscope
    summary = true
[push]
    default = simple
[filter "media"]
	clean = git-media-clean %f
	smudge = git-media-smudge %f
[difftool "sourcetree"]
	cmd = opendiff \"$LOCAL\" \"$REMOTE\"
    path = 

[mergetool "sourcetree"]
    cmd = /Applications/SourceTree.app/Contents/Resources/opendiff-w.sh \"$LOCAL\" \"$REMOTE\" -ancestor \"$BASE\" -merge \"$MERGED\"
    trustExitCode = true
[filter "lfs"]
	clean = git lfs clean %f
	smudge = git lfs smudge %f
	required = true
[rebase]
	autoStash = true
