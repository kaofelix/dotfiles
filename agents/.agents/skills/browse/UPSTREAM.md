# Provenance

Vendored from [`juanibiapina/browse-cli`](https://github.com/juanibiapina/browse-cli), path `skills/browse/SKILL.md`.

Upstream base: `dedc90d`. The vendored version includes the local retrospective-driven revision committed as `b175e1d` in the Pi-managed checkout; that commit was not pushed upstream.

This copy is maintained in dotfiles and discovered through `~/.agents/skills`. Pi's upstream Browse skill package is intentionally omitted to avoid duplicate loading. The Browse CLI and browser extension remain separately installed.

The upstream MIT license is retained in `LICENSE`.
